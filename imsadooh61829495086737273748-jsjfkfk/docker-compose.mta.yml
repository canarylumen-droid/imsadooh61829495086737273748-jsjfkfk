services:
  kumomta:
    image: ghcr.io/kumocorp/kumomta:latest
    restart: always
    ports:
      - "2525:2525"
    volumes:
      - ./kumomta/init.lua:/opt/kumomta/etc/policy/init.lua
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    restart: always
    volumes:
      - redis-data:/data

volumes:
  redis-data:
