---
description: Regenerate EC2 SSH key + push to EC2 Instance Connect
---

Run `node scripts/bootstrap-ec2.mjs` to regenerate the SSH key at `/tmp/audnix-ssh-key` and push it to EC2 Instance Connect. Run this at the start of every session before deploying.
